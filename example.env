# 100y-saas Environment Configuration
# Copy this file to .env and customize for your deployment

# ================================
# Core Application Settings
# ================================

# Database file path (relative to app binary or absolute)
# Default: data/app.db
DB_PATH=data/app.db

# Secret key for signing cookies and sessions
# IMPORTANT: Change this in production! Use a long random string
# Default: change-me
APP_SECRET=your-super-secret-key-change-me-in-production

# Base URL for the application (used in emails and redirects)
# Default: http://localhost:8080
BASE_URL=https://yourdomain.com

# ================================
# Email/SMTP Configuration
# ================================

# SMTP server hostname
# Default: localhost (development mode - emails will be logged instead of sent)
SMTP_HOST=smtp.gmail.com

# SMTP server port
# Default: 587
SMTP_PORT=587

# SMTP authentication username
# Default: empty (no authentication)
SMTP_USERNAME=your-email@gmail.com

# SMTP authentication password or app password
# Default: empty (no authentication)
SMTP_PASSWORD=your-app-password

# From address for outgoing emails
# Default: noreply@example.com
SMTP_FROM_ADDRESS=noreply@yourdomain.com

# From name for outgoing emails
# Default: 100y SaaS
SMTP_FROM_NAME=Your SaaS Name

# ================================
# Docker/Production Settings
# ================================

# Domain name for Caddy reverse proxy
# Used in docker-compose.yml
# Default: localhost
DOMAIN=yourdomain.com

# ================================
# Development Settings
# ================================

# Log level (DEBUG, INFO, WARN, ERROR, FATAL)
# Default: INFO
LOG_LEVEL=INFO

# Enable debug mode (more verbose logging)
# Default: false
DEBUG=false

# ================================
# Backup Configuration
# ================================

# Directory for database backups
# Default: backups
BACKUP_DIR=backups

# ================================
# Common SMTP Provider Examples
# ================================

# Gmail:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password (not your regular password!)

# SendGrid:
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USERNAME=apikey
# SMTP_PASSWORD=your-sendgrid-api-key

# Amazon SES:
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USERNAME=your-ses-smtp-username
# SMTP_PASSWORD=your-ses-smtp-password

# Mailgun:
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_USERNAME=your-mailgun-smtp-username
# SMTP_PASSWORD=your-mailgun-smtp-password

# ================================
# Security Notes
# ================================

# 1. Never commit .env to version control
# 2. Use strong, random values for APP_SECRET
# 3. Use app passwords, not regular passwords for email
# 4. Restrict SMTP credentials to minimum required permissions
# 5. Use HTTPS in production (BASE_URL should start with https://)
# 6. Consider using a dedicated SMTP service for better deliverability
